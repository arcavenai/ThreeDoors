# Story 17.8: Redesign Shoji Theme Visual Layout

**Status:** Done (PR #186)
**Epic:** 17 — Door Theme System
**Depends on:** Story 17.7
**Blocks:** None

## User Story

As a user,
I want the Shoji theme to evoke the elegance of a Japanese screen with large paper panes and thin wooden frames,
So that my task text is prominent and the decoration enhances rather than competes.

## Acceptance Criteria

**Given** the Shoji theme is selected
**When** three doors are rendered
**Then:**

- AC1: The grid uses fewer, larger panes — no more than 3-4 columns of lattice (down from the current ~8+ small cells)
- AC2: Task text occupies the majority of the door's visual area (content-to-decoration ratio visibly favors content)
- AC3: Lattice lines are thin (`─│┼`) — the frame feel is "wooden strips between paper", not "graph paper"
- AC4: Selected state uses heavy characters (`━┃╋`) and brighter color, providing clear visual distinction
- AC5: Minimum 15 characters of usable text width at `MinWidth`
- AC6: Word wrapping continues to function correctly for long task text
- AC7: Golden file tests updated with new layout
- AC8: Visual width consistency (from Story 17.7) holds for new layout

## Quality Gate

- QG1: `make fmt` passes (gofumpt formatted)
- QG2: `make lint` passes with zero warnings
- QG3: `make test` passes — all tests green
- QG4: Rebased on latest main
- QG5: Scope-checked against this story — no extras
- QG6: Errors handled with context wrapping (`%w`)

## Target Visual (28-char wide, unselected)

The redesigned shoji should feel like looking through a sliding screen — thin wooden
frame with large open paper panes where the task text lives. The grid signature comes
from a few horizontal lattice bars, NOT from subdividing every cell.

```
┬────────────────────────┬
│                        │
├────────────────────────┤
│                        │
│   Fix login bug        │
│                        │
├──────────┼─────────────┤
│                        │
│                        │
├────────────────────────┤
│                        │
┴────────────────────────┴
```

**Selected state** swaps to heavy chars (`━┃╋┳┻┣┫`) with brighter color `223`:

```
┳━━━━━━━━━━━━━━━━━━━━━━━━┳
┃                        ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━┫
┃                        ┃
┃   Fix login bug        ┃
┃                        ┃
┣━━━━━━━━━━╋━━━━━━━━━━━━━┫
┃                        ┃
┃                        ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━┫
┃                        ┃
┻━━━━━━━━━━━━━━━━━━━━━━━━┻
```

Key differences from current implementation:
- Side frame is a single `│` (not `│  │` with cell subdivisions)
- Horizontal bars at 3-4 fixed positions (top, content-top, mid-cross, content-bottom, bottom)
- One optional `┼` junction at the mid-cross bar to give the lattice feeling without overwhelming
- Content area is one large open pane — NO vertical subdivisions inside

## Technical Notes

- Current grid in `shoji.go` uses `cellW = 3` with `numCols = (width - 1) / cellW`, creating many small cells
- New approach: single-column content area with thin `│` side frame and 3-4 horizontal `─` lattice bars at fixed row positions
- The content pane should be a single large area, not subdivided into cells
- Preserve the `shojiChars` struct and selected/unselected character sets
- The lattice feel comes from horizontal bars and a single mid-cross junction `┼`, not from many vertical cell dividers
- Content-to-decoration ratio: at 28-char width, content area should be 24 chars wide (width minus 2 frame chars minus 2 padding chars)

## Tasks

1. Redesign `shojiRender` to use 3-column layout (frame | content | frame)
2. Replace dense grid with 2-3 horizontal lattice bars separating content rows
3. Ensure content pane uses full available width minus thin frame columns
4. Verify selected state contrast with heavy characters
5. Regenerate golden files
6. Verify all quality gates pass

## Pre-PR Checklist

- [ ] `make fmt` — no formatting changes needed
- [ ] `make lint` — zero warnings
- [ ] `make test` — all tests pass
- [ ] `go test -race ./...` — no race conditions
- [ ] Visual inspection: task text is prominent, lattice is minimal
- [ ] Content-to-decoration ratio favors content
