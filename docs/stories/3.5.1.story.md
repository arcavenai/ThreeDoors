<!--
title: "3.5.1: Core Domain Extraction"
status: "in-progress"
-->

# Story 3.5.1: Core Domain Extraction

**As a** developer,
**I want** `internal/tasks` split into `internal/core` (domain logic) and separate adapter packages,
**so that** the architecture follows the five-layer design specified in Architecture v2.0 and enables the Plugin SDK (Epic 7).

## Acceptance Criteria

1. **Core Domain Package**: `internal/core/` contains domain logic: Task, TaskPool, TaskStatus, DoorSelector, DoorSelection, StatusManager (task status transitions), SessionTracker, CompletionCounter, PatternAnalyzer, InsightsFormatter, MoodSelector, GreetingInsights, ValuesConfig, Onboarding, TagParser, TaskCategorization (TaskType, TaskEffort, TaskLocation), TaskImporter, ImprovementWriter, MetricsWriter, HealthChecker
2. **Provider Interface**: `internal/core/` exports the `TaskProvider` interface (and related types: Registry, AdapterFactory, ProviderConfig, ProviderEntry, FallbackProvider)
3. **TextFile Adapter**: `internal/adapters/textfile/` contains the YAML file adapter (extracted from FileManager + TextFileProvider)
4. **Apple Notes Adapter**: `internal/adapters/applenotes/` contains the Apple Notes adapter
5. **Obsidian Adapter**: `internal/adapters/obsidian/` contains the Obsidian adapter (including daily notes, watcher)
6. **Sync Infrastructure**: Sync-related code (SyncEngine, SyncState, SyncStatus, SyncManager, WriteQueue, WALProvider) stays in `internal/tasks/` for now OR moves to `internal/sync/` per architecture — this story focuses on core + adapters split
7. **Dependency Inversion**: `internal/tui/` depends only on `internal/core/`, not on adapter implementations
8. **All existing tests pass** without modification (behavior-preserving refactor)
9. **No user-facing behavior changes**

## NOT In Scope

* TaskProvider interface hardening (Watch, HealthCheck, ChangeEvent) — that's Story 3.5.2
* Config.yaml schema changes — that's Story 3.5.3
* Apple Notes adapter error handling improvements — that's Story 3.5.4
* New test suites — that's Story 3.5.5
* Moving sync infrastructure to `internal/sync/` — can be deferred

## Scope Reality Check

The original AC says "~2,100 LOC across 12 files" but `internal/tasks/` actually has:
- 25 source files (~14K LOC)
- 51 test files (~4K+ LOC)
- 76 total files (~18K LOC)
- 42 files across the codebase import `internal/tasks/`

This is a significant refactor. The approach must be incremental and behavior-preserving.

## Definition of Done

* All acceptance criteria implemented
* `make build` succeeds with zero errors
* `make test` passes with zero failures
* `gofumpt -d .` produces no output (code is formatted)
* `golangci-lint run ./...` passes with zero warnings
* `go test -race ./...` passes
* No user-facing behavior changes

### Pre-PR Submission Checklist

- [ ] **Rebase onto latest main**: `git fetch upstream main && git rebase upstream/main`
- [ ] **Run gofumpt**: `gofumpt -l .` — no output
- [ ] **Run golangci-lint**: `golangci-lint run ./...` — 0 issues
- [ ] **Run all tests**: `go test ./... -count=1` — 0 failures
- [ ] **Race detector**: `go test -race ./...` — 0 data races
- [ ] **Verify no out-of-scope files**: `git diff --stat` — only files related to this story
- [ ] **Build succeeds**: `make build` — clean build

### Dev Notes

**Strategy: Package Aliasing Approach**

Rather than moving all files at once (which would break 42 importers), use a phased approach:

1. Create `internal/core/` package with the domain types
2. Create `internal/adapters/{textfile,applenotes,obsidian}/` packages
3. Move domain types to `internal/core/`, adapter types to adapter packages
4. Update all imports across the codebase (42 files)
5. Keep `internal/tasks/` as a thin re-export shim if needed during transition, or remove entirely
6. Register adapters in `cmd/threedoors/main.go` using new package paths

**Key constraint**: The `internal/adapters/` directory already exists with contract test files (`contract.go`, `contract_test.go`, `obsidian_contract_test.go`). These must be preserved/integrated.

**Five-Layer Target** (from Architecture v2.0):
1. TUI Layer (`internal/tui`) — Bubbletea views
2. Core Domain (`internal/core`) — Task management, door selection, progress tracking
3. Adapter Layer (`internal/adapters`) — Pluggable TaskProvider implementations
4. Sync Engine (`internal/sync`) — Offline-first queue, conflict resolution
5. Intelligence Layer (`internal/intelligence`) — LLM, calendar, learning

### Parent Epic

* Parent Epic: Epic 3.5: Architecture v2.0 Foundation & Technical Debt Resolution

### Architecture & Design

**Dependency Flow (after refactor):**
```
cmd/threedoors/main.go
  → internal/core (domain types, interfaces)
  → internal/adapters/{textfile,applenotes,obsidian} (implementations)
  → internal/tui (depends on core only)
```

**Core Domain types to extract:**
- `Task`, `TaskNote`, `NewTask()`, `NewTaskWithContext()` (from task.go)
- `TaskStatus`, `IsValidTransition()`, `GetValidTransitions()`, `ValidateStatus()` (from task_status.go)
- `TaskPool`, `NewTaskPool()` (from task_pool.go)
- `TaskProvider` interface (from provider.go)
- `DoorSelector`, `DoorSelection` (from door_selector.go, door_selection.go)
- `SessionTracker`, `SessionData` (from session_tracker.go)
- `CompletionCounter` (from completion_counter.go)
- `PatternAnalyzer`, `PatternReport`, etc. (from pattern_analyzer.go)
- `InsightsFormatter` (from insights_formatter.go)
- `MoodSelector` (from mood_selector.go)
- `GreetingInsights` (from greeting_insights.go)
- `ValuesConfig` (from values_config.go)
- `Onboarding` (from onboarding.go)
- `TaskType`, `TaskEffort`, `TaskLocation` (from task_categorization.go)
- `TagParser` (from tag_parser.go)
- `TaskImporter` (from task_importer.go)
- `HealthChecker`, `HealthCheckResult` (from health_checker.go)
- `ImprovementWriter` (from improvement_writer.go)
- `MetricsWriter` (from metrics_writer.go)
- `Registry`, `AdapterFactory`, `ProviderConfig`, `ProviderEntry` (from registry.go, provider_config.go)
- `FallbackProvider` (from fallback_provider.go)

**Adapter packages to create:**
- `internal/adapters/textfile/` — TextFileProvider + file_manager functions
- `internal/adapters/applenotes/` — AppleNotesProvider
- `internal/adapters/obsidian/` — ObsidianAdapter + DailyNotesConfig + ObsidianWatcher

**Sync types (stay in tasks or move to sync later):**
- SyncEngine, SyncState, SyncStatus, SyncManager, WriteQueue, WALProvider

### Key Files to Create/Modify

| File | Action |
|------|--------|
| `internal/core/*.go` | Created — domain types extracted from internal/tasks |
| `internal/adapters/textfile/*.go` | Created — TextFileProvider extracted |
| `internal/adapters/applenotes/*.go` | Created — AppleNotesProvider extracted |
| `internal/adapters/obsidian/*.go` | Created — ObsidianAdapter extracted |
| `internal/tasks/*.go` | Modified/Removed — types moved out |
| `internal/tui/*.go` | Modified — imports updated to internal/core |
| `cmd/threedoors/main.go` | Modified — imports updated |
| `internal/enrichment/*.go` | Modified — imports updated |
| `internal/intelligence/llm/*.go` | Modified — imports updated |
| `internal/calendar/*.go` | Modified — imports updated |

### Testing

* All existing tests must pass — this is a behavior-preserving refactor
* Tests move with their source files to new packages
* Contract tests in `internal/adapters/` integrate with new adapter packages
* Run `go test -race ./...` to catch any concurrency issues from refactoring

## Tasks / Subtasks

### Phase 1: Create core package
- [ ] Create `internal/core/` directory
- [ ] Move `task.go` → `internal/core/task.go` (Task, TaskNote, NewTask, etc.)
- [ ] Move `task_status.go` → `internal/core/task_status.go`
- [ ] Move `task_pool.go` → `internal/core/task_pool.go`
- [ ] Move `provider.go` → `internal/core/provider.go` (TaskProvider interface)
- [ ] Move `door_selector.go` → `internal/core/door_selector.go`
- [ ] Move `door_selection.go` → `internal/core/door_selection.go`
- [ ] Move `task_categorization.go` → `internal/core/task_categorization.go`
- [ ] Move remaining domain types to `internal/core/`

### Phase 2: Create adapter packages
- [ ] Create `internal/adapters/textfile/` with TextFileProvider + file_manager
- [ ] Create `internal/adapters/applenotes/` with AppleNotesProvider
- [ ] Create `internal/adapters/obsidian/` with ObsidianAdapter + daily + watcher
- [ ] Move corresponding test files

### Phase 3: Update imports
- [ ] Update all `internal/tui/` imports from `tasks` → `core`
- [ ] Update `cmd/threedoors/main.go` imports
- [ ] Update `internal/enrichment/` imports
- [ ] Update `internal/intelligence/llm/` imports
- [ ] Update `internal/calendar/` imports
- [ ] Update `internal/adapters/contract.go` imports
- [ ] Update `internal/dist/` imports

### Phase 4: Handle remaining tasks package
- [ ] Move sync types to stay in tasks or create thin wrappers
- [ ] Move registry/config to core
- [ ] Remove or minimize `internal/tasks/` package
- [ ] Register adapters from new package paths

### Phase 5: Verify
- [ ] `make build` passes
- [ ] `make test` passes
- [ ] `make lint` passes
- [ ] `make fmt` produces no changes
- [ ] `go test -race ./...` passes
- [ ] No user-facing behavior changes
